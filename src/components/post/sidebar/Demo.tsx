import { PostType } from "@/types";
import styles from './PostCard.module.css'

export default function Demo({post}:{post:PostType}) {
  if(false) {
    return false;
  }
  return (
    <aside>
      <p>Demo</p>
      <p>
        DEMO
      </p>
    </aside>
  )
}